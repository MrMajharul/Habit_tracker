import { ComingSoon } from "@/components/shared/coming-soon";

export const metadata = { title: "Hadith" };

export default function HadithPage() {
  return (
    <ComingSoon
      title="Hadith"
      description="Browse verified Hadith, bookmark favorites, and share — Phase 4."
    />
  );
}
