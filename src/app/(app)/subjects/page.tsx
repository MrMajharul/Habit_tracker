import { SubjectsPageClient } from "@/components/subjects/subjects-page-client";

export const metadata = {
  title: "Subjects & Work Areas — Istiqamaah",
  description: "Organize your study and work subjects with weekly targets.",
};

export default function SubjectsPage() {
  return <SubjectsPageClient />;
}
