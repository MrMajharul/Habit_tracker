import { ComingSoon } from "@/components/shared/coming-soon";

export const metadata = { title: "Prayer" };

export default function PrayerPage() {
  return (
    <ComingSoon
      title="Prayer"
      description="Full prayer tracking with live calculation, notifications, and settings arrives in Phase 2. The dashboard already shows mock prayer times behind a service abstraction."
    />
  );
}
